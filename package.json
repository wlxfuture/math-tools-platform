{
  "name": "math-tools-platform",
  "version": "1.0.0",
  "description": "Production-ready math tools platform with Cloudflare Pages, Workers, and KV storage",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --local",
    "deploy:pages": "wrangler pages deploy public --project-name math-tools-platform",
    "deploy": "npm run deploy:pages",
    "kv:create": "wrangler kv:namespace create STATS && wrangler kv:namespace create STATS --preview",
    "kv:list": "wrangler kv:key list",
    "logs": "wrangler tail"
  },
  "keywords": [
    "cloudflare",
    "pages",
    "workers",
    "kv",
    "math",
    "calculator",
    "analytics"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.28.0"
  }
}
